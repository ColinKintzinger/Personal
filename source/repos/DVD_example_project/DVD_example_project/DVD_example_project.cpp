// DVD_example_project.cpp : This file contains the 'main' function. Program execution begins and ends there.
//

#include "Header.h"
int main()
{
    int total;
    cout << "Enter the total number of records: ";
    cin >> total;
    /*int* id = nullptr;
    string* name = nullptr;
    double* cost = nullptr;*/
    Movie* movieList = nullptr;
    /* int total = readFile(id, name, cost);
     print(id, name, cost, total);
     sortFile(id, name, cost, total);
     print(id, name, cost, total);*/
    return 0;
}


